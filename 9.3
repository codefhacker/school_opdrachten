import json
import requests

stalling = requests.get("https://stallingsnet.nl/api/1/parkingcount/utrecht")
infoStalling = json.loads(stalling.text)
data = json.dumps(stalling.text, indent=2)

print(str(data))